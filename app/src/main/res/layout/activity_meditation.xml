<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MeditationActivity">

    <TextView
        android:id="@+id/T1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="50dp"
        android:layout_marginTop="50dp"
        android:text="Meditation"
        android:textColor="@color/black"
        android:textSize="30sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/T1Desc1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/T1"
        android:layout_marginStart="50dp"
        android:layout_marginTop="5dp"
        android:paddingStart="0dp"
        android:paddingEnd="60dp"
        android:text="Let's start with that daily mediation activity being with yourself let all of your thoughts flow through" />

    <ImageButton
        android:id="@+id/StartBtn"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_below="@id/T1Desc1"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="165dp"
        android:background="@null"
        android:contentDescription="Play/Pause"
        android:scaleType="fitCenter"
        android:src="@drawable/playbutton" />



</RelativeLayout>